<?php

$module = $getModule();

// are there any sessions in the DB we can clean up

global $db, $models;
/*
  $sesRes = $db->find($models['session'], array('criteria' => array(
    array('session', '=', $sid),
  )));
*/

?>
